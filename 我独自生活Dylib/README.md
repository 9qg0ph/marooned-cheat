# 我独自生活修改器 v15.0

## 📦 最新版本

**WoduziCheat.m v15.0** - Unity内存数据拦截系统（2026年1月20日更新）

### 🆕 v15.0 重大更新
- 🎮 **Unity内存拦截系统**：完全重构为针对Unity引擎的内存拦截机制
- 🔧 **Hook memcpy/memmove**：拦截底层内存操作函数，捕获游戏数值读取
- 🎯 **Unity PlayerPrefs Hook**：拦截Unity标准数据存储API
- 🧠 **智能数值识别**：基于数值范围自动识别游戏属性（金钱、体力、健康、心情）
- 📊 **实时拦截统计**：显示拦截次数和效果，提供详细监控信息
- ⚡ **解决v14.4问题**：v14.4只能拦截广告SDK数据，v15.0专门针对游戏数据

## 🎯 功能特点

- ✅ **Unity内存拦截**：专门针对Unity引擎游戏优化
- ✅ **智能数值识别**：基于数值范围自动识别游戏属性
- ✅ **实时拦截替换**：检测到目标数值时自动替换
- ✅ **多层Hook机制**：memcpy + memmove + Unity PlayerPrefs
- ✅ **详细监控日志**：记录所有内存操作和拦截行为
- ✅ **版权信息和图标URL加密保护**

## 🎮 支持功能

| 功能 | 说明 | 修改值 |
|------|------|--------|
| 💰 无限金钱 | Unity内存拦截金钱数值 | 999,999,999 |
| ⚡ 无限体力 | Unity内存拦截体力数值 | 999,999 |
| ❤️ 无限健康 | Unity内存拦截健康数值 | 999 |
| 😊 无限心情 | Unity内存拦截心情数值 | 999 |
| 🎁 一键全开 | 同时启用所有Unity拦截器 | 全部最大值 |
| 🎮 Unity状态 | 查看拦截状态和统计信息 | - |

## 📝 使用方法

1. 下载编译好的 `WoduziCheat.dylib`
2. 使用注入工具注入到游戏
3. 启动游戏，看到 **独** 字悬浮按钮（会自动加载科技虎图标）
4. 点击按钮，首次使用需同意免责声明
5. 选择要开启的功能，确认修改
6. **在游戏中进行操作**（购买、使用体力等）触发数值读取
7. 系统会自动拦截并替换数值
8. 查看日志了解拦截效果

## 🔧 编译方法

### GitHub Actions 自动编译
推送代码到 GitHub 后会自动编译，下载 Artifacts 中的 dylib 文件。

### 手动编译
```bash
clang -arch arm64 \
  -isysroot $(xcrun --sdk iphoneos --show-sdk-path) \
  -miphoneos-version-min=14.0 \
  -dynamiclib \
  -framework UIKit \
  -framework Foundation \
  -fobjc-arc \
  -o WoduziCheat.dylib \
  WoduziCheat.m

ldid -S WoduziCheat.dylib
```

## 💡 技术细节

### Unity内存拦截机制
1. **memcpy Hook**：拦截内存复制操作，识别游戏数值读取
2. **memmove Hook**：拦截内存移动操作，捕获数据传输
3. **Unity PlayerPrefs Hook**：拦截Unity标准数据存储
4. **智能数值识别**：基于数值范围自动识别游戏属性

### 数值识别范围
```objective-c
// 智能数值识别算法
- 金钱: 100-100,000 范围
- 体力: 100-500 范围  
- 健康/心情: 50-200 范围
```

### Hook实现原理
```objective-c
// memcpy Hook实现
static void* hooked_memcpy(void *dest, const void *src, size_t n) {
    void* result = original_memcpy(dest, src, n);
    
    // 检查4字节整数读取
    if (n == sizeof(int) && g_unityHookEnabled) {
        int value = *(int*)src;
        // 智能识别并替换数值
        if (value >= 100 && value <= 100000) {
            *(int*)dest = (int)g_modifiedMoney;
        }
    }
    return result;
}
```

### 数据结构分析
基于用户测试发现的固定偏移关系：
- **金钱地址**：基地址 + 0字节
- **体力地址**：基地址 + 24字节 (+0x18)
- **健康地址**：基地址 + 72字节 (+0x48)
- **心情地址**：基地址 + 104字节 (+0x68)

## 🛠️ 版本历史

### v15.0 (2026-01-20) - Unity内存数据拦截系统
- 🎮 完全重构为Unity内存拦截系统
- 🔧 Hook memcpy/memmove内存操作函数
- 🎯 Hook Unity PlayerPrefs数据存储
- 🧠 智能识别游戏数值范围，自动拦截替换
- 📊 实时拦截统计和效果反馈
- ⚡ 解决v14.4只能拦截广告SDK数据的问题

### v14.4 (2026-01-20) - 全方位Hook监控引擎
- 🔍 扩展Hook覆盖范围到NSDictionary和NSMutableDictionary
- 📝 记录所有数据读取操作，智能关键词匹配
- ⚠️ 问题：只能拦截广告SDK数据，无法拦截游戏核心数据

### v14.3 (2026-01-20) - Hook拦截引擎  
- 🔧 采用Hook拦截方式替代直接内存修改
- ✅ 解决了v14.2的闪退问题
- 🎯 Hook NSUserDefaults的objectForKey和integerForKey方法
- ⚠️ 问题：Hook安装成功但无法拦截游戏数据

### v14.0-v14.2 - 智能内存修改系统
- v14.0: iOS兼容内存搜索引擎（搜索超时问题）
- v14.1: 性能优化版本（仍有超时）
- v14.2: 防闪退保护版本（引起闪退）

### v13.0 - 动态地址适应版本
- 🔄 实现ASLR地址动态适应
- 📍 基于偏移量的数据结构识别
- ⚠️ 问题：地址计算不准确

### v10.0-v12.0 - 直接内存修改尝试
- v10.0-v11.0: 使用mach API直接修改内存（编译错误）
- v12.0: 安全内存访问版本（引起闪退）

### v6.0-v9.0 - 存档文件修改尝试
- v6.0: Unity存档检测系统
- v7.0-v9.0: 智能内存修改指导助手
- ⚠️ 问题：无法找到实际存档文件

### v1.0-v5.2 - NSUserDefaults修改尝试
- 📝 基于NSUserDefaults和ES3Save JSON字符串替换
- ⚠️ 问题：用户确认NSUserDefaults不控制游戏数据

## ⚠️ 注意事项

1. **游戏中操作**：启用拦截器后，在游戏中进行操作（购买、使用体力等）触发数值读取
2. **查看日志**：详细日志位置：`/var/mobile/Documents/woduzi_cheat.log`
3. **拦截统计**：使用"Unity状态"功能查看拦截次数和效果
4. **重启游戏**：地址会变化但拦截器会自动适应
5. **故障排除**：如果拦截无效，尝试不同的游戏操作触发数值变化

## 🔍 故障排除

### 如果Unity拦截无效：
1. 确保在游戏中进行操作（购买、使用体力等）
2. 查看日志确认是否有内存读取记录
3. 检查"Unity状态"中的拦截统计
4. 游戏可能使用其他数据存储方式（SQLite、文件等）

### 如果出现闪退：
1. v15.0已解决闪退问题
2. 如仍有问题，查看异常日志
3. 尝试单独启用功能而非一键全开

## 🔒 安全保护

- 版权信息 Base64 编码 + 动态拼接
- 图标URL Base64 编码 + 动态拼接  
- 防止二进制文件修改版权和图标
- 安全的内存访问方法，避免游戏崩溃
- 全局异常处理器，防闪退保护

## 📄 免责声明

本工具仅供技术研究与学习，严禁用于商业用途及非法途径。使用本工具修改游戏可能违反游戏服务条款，用户需自行承担一切风险和责任。严禁倒卖、传播或用于牟利，否则后果自负。

## 📄 License

© 2026 𝐈𝐎𝐒𝐃𝐊 科技虎