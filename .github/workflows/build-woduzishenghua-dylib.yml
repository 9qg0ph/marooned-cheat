name: Build WoduziShenghua Dylib

on:
  push:
    paths:
      - '我独自生活Dylib/**'
      - '.github/workflows/build-woduzishenghua-dylib.yml'
  pull_request:
    paths:
      - '我独自生活Dylib/**'
      - '.github/workflows/build-woduzishenghua-dylib.yml'

jobs:
  build:
    runs-on: macos-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup Xcode
      uses: maxim-lobanov/setup-xcode@v1
      with:
        xcode-version: latest-stable
    
    - name: Build WoduziShenghua Dylib (Simple Version)
      run: |
        cd 我独自生活Dylib
        clang -arch arm64 \
          -isysroot $(xcrun --sdk iphoneos --show-sdk-path) \
          -miphoneos-version-min=14.0 \
          -dynamiclib \
          -framework UIKit \
          -framework Foundation \
          -framework CoreGraphics \
          -fobjc-arc \
          -o WoduziCheat_Simple.dylib \
          WoduziCheat_Simple.m
        
        # Verify the build
        file WoduziCheat_Simple.dylib
        ls -la WoduziCheat_Simple.dylib
    
    - name: Build WoduziShenghua Dylib (RealTime Version)
      run: |
        cd 我独自生活Dylib
        clang -arch arm64 \
          -isysroot $(xcrun --sdk iphoneos --show-sdk-path) \
          -miphoneos-version-min=14.0 \
          -dynamiclib \
          -framework UIKit \
          -framework Foundation \
          -framework CoreGraphics \
          -fobjc-arc \
          -o WoduziCheat_RealTime.dylib \
          WoduziCheat_RealTime.m
        
        # Verify the build
        file WoduziCheat_RealTime.dylib
        ls -la WoduziCheat_RealTime.dylib
    
    - name: Upload Dylib
      uses: actions/upload-artifact@v4
      with:
        name: WoduziShenghua-Simple-Dylib
        path: 我独自生活Dylib/WoduziCheat_Simple.dylib
        retention-days: 30
    
    - name: Upload RealTime Dylib
      uses: actions/upload-artifact@v4
      with:
        name: WoduziShenghua-RealTime-Dylib
        path: 我独自生活Dylib/WoduziCheat_RealTime.dylib
        retention-days: 30