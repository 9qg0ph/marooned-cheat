name: Build TianXuan DaGongRen iOS Dylib

on:
  push:
    paths:
      - '天选打工人Dylib/**'
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup Xcode
      uses: maxim-lobanov/setup-xcode@v1
      with:
        xcode-version: latest-stable
    
    - name: Build dylib
      run: |
        cd 天选打工人Dylib
        
        # 编译为 dylib
        clang -arch arm64 \
          -isysroot $(xcrun --sdk iphoneos --show-sdk-path) \
          -miphoneos-version-min=14.0 \
          -dynamiclib \
          -framework UIKit \
          -framework Foundation \
          -framework CoreGraphics \
          -fobjc-arc \
          -Wno-deprecated-declarations \
          -o TianXuanDaGongRenCheat.dylib \
          TianXuanDaGongRenCheat.m
        
        # 签名
        ldid -S TianXuanDaGongRenCheat.dylib || codesign -f -s - TianXuanDaGongRenCheat.dylib
        
        echo "Build completed!"
        ls -la *.dylib
    
    - name: Upload dylib
      uses: actions/upload-artifact@v4
      with:
        name: TianXuanDaGongRenCheat-dylib
        path: 天选打工人Dylib/TianXuanDaGongRenCheat.dylib
