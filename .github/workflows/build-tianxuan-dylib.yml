name: Build TianXuan DaGongRen iOS Dylib

on:
  push:
    paths:
      - '天选打工人Dylib/**'
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup Xcode
      uses: maxim-lobanov/setup-xcode@v1
      with:
        xcode-version: latest-stable
    
    - name: Build dylib
      run: |
        cd 天选打工人Dylib
        
        # 编译天选打工人修改器
        clang -arch arm64 \
          -isysroot $(xcrun --sdk iphoneos --show-sdk-path) \
          -miphoneos-version-min=14.0 \
          -dynamiclib \
          -framework UIKit \
          -framework Foundation \
          -lsqlite3 \
          -fobjc-arc \
          -Wno-deprecated-declarations \
          -o TianXuanCheat.dylib \
          TianXuanCheat.m
        
        # 签名
        ldid -S TianXuanCheat.dylib || codesign -f -s - TianXuanCheat.dylib
        
        echo "✅ 天选打工人修改器编译完成!"
        ls -la *.dylib
    
    - name: Upload dylib
      uses: actions/upload-artifact@v4
      with:
        name: TianXuanCheat-dylib
        path: 天选打工人Dylib/TianXuanCheat.dylib
